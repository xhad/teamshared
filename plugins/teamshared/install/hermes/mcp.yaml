# teamshared — merge under mcp_servers: in ./.hermes/config.yaml (project-local)
  teamshared:
    url: __MCP_URL__
    headers:
      Authorization: "Bearer __TEAMSHARED_TOKEN__"
    timeout: 30
    connect_timeout: 10
