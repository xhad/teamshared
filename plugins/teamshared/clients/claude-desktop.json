{
  "_comment": "Merge into claude_desktop_config.json (macOS: ~/Library/Application Support/Claude/claude_desktop_config.json). Restart Claude Desktop after editing.",
  "mcpServers": {
    "teamshared-remote": {
      "_comment": "Preferred when teamshared is deployed on Tailscale/Railway",
      "url": "https://memory.tailXXXX.ts.net/mcp",
      "headers": {
        "Authorization": "Bearer <agent-token>"
      }
    },
    "teamshared-local": {
      "_comment": "Local stdio — requires teamshared on PATH and backing stores running",
      "command": "teamshared",
      "args": ["serve", "--transport", "stdio"],
      "env": {
        "TEAMSHARED_AUTH_DISABLED": "true",
        "TEAMSHARED_REDIS_URL": "redis://localhost:6379/0",
        "TEAMSHARED_PG_HOST": "localhost",
        "TEAMSHARED_PG_PORT": "5433"
      }
    }
  }
}
