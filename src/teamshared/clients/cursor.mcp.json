{
  "_comment": "Copy into ~/.cursor/mcp.json. Replace <agent-token> with the value from `teamshared token mint cursor`.",
  "mcpServers": {
    "teamshared-memory": {
      "url": "https://memory.tailXXXX.ts.net/mcp",
      "headers": {
        "Authorization": "Bearer <agent-token>"
      }
    }
  }
}
