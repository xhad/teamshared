{
  "_comment": "Copy into ~/.cursor/mcp.json. Replace <agent-token> with the value from `actx token mint cursor`.",
  "mcpServers": {
    "actx-memory": {
      "url": "https://memory.tailXXXX.ts.net/mcp",
      "headers": {
        "Authorization": "Bearer <agent-token>"
      }
    }
  }
}
